<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ name }}</title>
  <link rel="stylesheet" href="/app.dist.css">
</head>
<body>
  {{#each components}}
  <div class="component">
    <h1>{{titleize name}}</h1>
    <p>{{ description }}</p>
    {{link name}}
  </div>
  {{/each}}
</body>
</html>
